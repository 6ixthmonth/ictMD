<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sesoc.ictmd.Interface.MiscDAO">

	<select id="getMarkerList" resultType="Marker">
		select
			ct.alpha_two_code as "alphaTwoCode",
			ct.country_name as "countryName",
			mt.landmark,
			mt.latitude,
			mt.longitude,
			mt.img_url as "imgUrl"
		from
			country_code_tb ct, earth_marker_tb mt
		where
			ct.alpha_two_code = mt.alpha_two_code
		order by
			ct.country_name asc
	</select>

</mapper>